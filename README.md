# Contador Regresivo Edge

Proyecto de ejemplo que expone una Edge Function en `/api/countdown.svg` para generar contadores regresivos en formato SVG para emails.

Comandos:

```bash
npm install
npm run dev
```
